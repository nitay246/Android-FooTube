<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ADVANCED_SYSTEM_PROGRAMING"
        tools:targetApi="31">

        <activity
            android:name=".Edit_activity"
            android:exported="true">
        </activity>

        <activity
            android:name=".upload_activity"
            android:exported="true">
        </activity>
        <activity
            android:name=".Watch"
            android:exported="true">

        </activity>

        <activity
            android:name=".test_watch"
            android:exported="true">
            <!-- Add an intent filter if this activity is also a launcher -->
        </activity>
        <activity
            android:name=".testing_watch"
            android:exported="true">
            <!-- Add an intent filter if this activity is also a launcher -->
        </activity>

        <activity
            android:name=".signup"
            android:exported="true" />
        <activity
            android:name=".intro_activity"
            android:exported="true" />
        <activity
            android:name=".HomepageActivity"
            android:exported="true" />


        <activity
            android:name=".Signin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>